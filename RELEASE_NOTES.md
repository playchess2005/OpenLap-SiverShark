# OpenLap v0.4.0

本版本重点完善 Studio 的 BLF/DBC 工作流、手动对齐能力和导出可靠性。

## 新功能

- 新增独立的 Signal 轨迹选择页面：完整列出扫描结果，并支持按 Message、Signal、CAN ID 和 DBC 文件名过滤。
- 支持任意选择多条 BLF Signal 轨迹、自定义轨迹名称，并在 Studio 时间轴中显示。
- BLF 时间轴支持滚轮缩放、拖动平移、关键帧添加、选择、删除，以及关键帧与当前视频位置一键对齐。
- BLF 通道扫描、Signal 扫描和轨迹读取均显示百分比、帧数、已用时间和预计剩余时间；完成后以绿色显示扫描结果。
- Overlay 的 Signal Source 选择器支持搜索过滤。
- 四轮扭矩仪表支持 FL、FR、RL、RR 四个独立 Error Signal：正常为绿色，报错为红色。

## 性能与稳定性

- 为 BLF 通道和 Signal 扫描增加缓存与并发保护，避免重复点击启动多个扫描任务。
- Studio 与 Overlay 往返时复用同一个轨迹读取任务及其结果，不再出现两个进度条或重新扫描整个 BLF。
- 轨迹读取只解码所选 Signal 对应的 CAN 帧，并对显示数据降采样。
- 源码运行时根据前端文件修改时间刷新缓存，避免 WebView2 使用旧版 JS/CSS。
- 本地视频服务忽略浏览器主动中止的连接，避免重复输出 WinError 10053。

## 导出修复

- 源码安装自动提供 FFmpeg，并在打包版本中正确包含 FFmpeg、BLF/DBC 与 GPMF 相关依赖。
- 改进 Studio 导出进度、失败日志和输出文件校验。
- 无 GoPro GPMF 遥测轨迹的视频不再阻断手动 BLF 对齐导出。
- 修复导出脚本模块路径、FFmpeg 查找和音频混流相关错误。
